import 'package:flutter/material.dart';

class CamersScreen extends StatelessWidget {
  const CamersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Camera'),
    );
  }
}
